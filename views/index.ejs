<% include ./partials/header   %>

<div class="ui main text container">
    <div class="ui huge container">Index</div>
    <div class="ui top attached segment shadowed">
        <div class="ui divided items">
            <% blogs.forEach(blog =>{ %>
                <div class="item">
                    <div class="image">
                        <img class="shadowed" src="<%= blog.image %>" alt="">
                        <div class="meta">
                            <span><%= blog.created.toDateString() %></span>
                        </div>
                    </div>
                    <div class="content">
                        <a class="header" href="/blogs/<%= blog._id %>"><%-blog.title%></a>
                        <div class="description">
                            <p>
                                <%-blog.body.substring(0, 100)%>...
                            </p>
                        </div>
                        <div class="extra">
                            <!-- <a class="ui floated basic green button shadowed" href="/blogs/<%= blog._id %>">Read More<i class="right chevron icon"></i></a> -->
                            <a class="ui basic button shadowed" href="/blogs/<%= blog._id %>">Read More<i class="right chevron icon"></i></a>
                        </div>

                    </div>

                </div>
                <% }) %>
        </div>
    </div>
</div>

<% include ./partials/footer   %>