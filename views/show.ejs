<% include ./partials/header %>

<div class="ui main text container segment shadowed">

    <div class="ui huge header"><%= blog.title %></div>
    <div class="ui top attached>   
        <div class="item"> 
            <img class="ui centered rounded image shadowed" src="<%= blog.image %>" alt="">
            <div class="content">
                <span><%= blog.created.toDateString() %></span>

            </div>
            <div class="description ui segment">
                <!-- the Minus allows code entry that runs -->
                    <p><%- blog.body %></p> 

            </div>
            <a class="ui basic button shadowed" href="/blogs/<%=blog._id %>/edit">EDIT</a>
            <form id="delete" method="POST" action="/blogs/<%=blog._id %>?_method=DELETE">
                <button class="ui basic button shadowed">
                    DELETE
                </button>

            </form>
        </div>
    </div>
</div>

<% include ./partials/footer %>